apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    volume: aai-db-claim0
  name: aai-db-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
